## Mismatch Fixture Pack

This fixture pack is intended to exercise preflight mismatch behavior.

Included files:

- `crop_by_season.csv`: references a crop not present in `crop_info.csv`, which should produce an error outcome.
